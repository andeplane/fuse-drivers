# The image needs the sources, tracks and lockfile; nothing else. Without this a --source deploy
# would upload the 98 MB history and the 43 MB of raw art the build never reads.
.git
.github
node_modules
dist
assets
docs
tests
artifacts
*.md
